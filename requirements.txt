# Friday — core runtime (VM: install into .venv via scripts/vm_cycle.sh deploy)
fastapi>=0.111
uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2.7
numpy>=1.26
python-multipart>=0.0.9
pyyaml>=6.0
aiofiles>=23.2
websockets>=12.0
edge-tts>=6.1
pypdf>=4.2
pytest>=8.2
pytest-asyncio>=0.23

# --- Optional, installed on VM only (not required for core boot) ---
# fastembed>=0.3.6        # bge-small 384d local embeddings (FRIDAY-Δ SENSE ①)
# sqlite-vec>=0.1.6       # not used — FRIDAY-Δ replaces vec0 with in-RAM binary prefilter
# onnxruntime>=1.18       # INT8 MiniLM-L6 cross-encoder reranker (SENSE ⑥)
# pdf2image>=1.17         # OCR fallback for scanned books (Books)
# pytesseract>=0.3.10     # OCR engine wrapper (Books)
# docling>=2.0            # heavy OCR pipeline (Books, VM only)
# tiktoken>=0.7           # token accounting
eval_type_backport>=0.2.0
